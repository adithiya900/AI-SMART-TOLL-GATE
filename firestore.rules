rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all reads and writes (dev/demo mode)
      // For production: change to: allow read, write: if request.auth != null;
      allow read, write: if true;
    }
  }
}
